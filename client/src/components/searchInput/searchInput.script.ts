export default {
	name: "SearchInput",
	// setup({ emit }) {
	// 	const inputValue = ref("");

	// 	function onInput(event: Event) {
	// 		emit("update:modelValue", (event.target as HTMLInputElement).value);
	// 	}

	// 	function onSearch() {
	// 		emit("search", inputValue.value);
	// 	}

	// 	return { inputValue, onInput, onSearch };
	// }
};